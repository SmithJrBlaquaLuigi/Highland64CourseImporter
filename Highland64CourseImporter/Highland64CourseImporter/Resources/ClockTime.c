#include <conio.h>
#include <Graphics.h>
#include <Time.h>
#include <dos.h>
#include <string.h>
    int main(){
        int gd = DETECT, gm;
        int midx, midy;
        long current_time;
        char timeStr[256];

        initgraph(&gd, &gm, "C:\Users\SmithJrBlaquaLuigi");

        /* mid pixeld in horizontal and Vertical Axis Position */
        midx = getmaxx() / 2;
        midy = getmaxy() / 2;
        process();

        if getmaxx = !null;
        else
        if getmaxy = !null;
        while (!kbhit()) {
            cleardevice();
            SetColor(Blue);
            Setfillstyle(SOLID_FILL, WHITE);
        }
    }